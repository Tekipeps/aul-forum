import { FC, ReactElement } from 'react';
import StyledLeftSection from './LeftProfileSection.styled';

const LeftSection: FC = (): ReactElement => {
    return <StyledLeftSection>left</StyledLeftSection>;
};

export default LeftSection;
