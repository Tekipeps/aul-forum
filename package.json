{
    "dependencies": {
        "@prisma/client": "2.17.0",
        "bcrypt": "^5.0.1",
        "express": "^4.17.1"
    },
    "scripts": {
        "start": "set NODE_ENV=production && node index.js",
        "dev": "set NODE_ENV=development && nodemon index.js",
        "dev:db:up": "prisma db push --preview-feature --accept-data-loss --force-reset && prisma generate && yarn db:seed",
        "db:seed": "node prisma/seed.js"
    },
    "devDependencies": {
        "dotenv": "^8.2.0",
        "faker": "^5.4.0",
        "nodemon": "^2.0.7",
        "prisma": "2.17.0"
    }
}
